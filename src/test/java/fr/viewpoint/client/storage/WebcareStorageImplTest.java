package fr.viewpoint.client.storage;

import fr.viewpoint.client.Configuration;

public class WebcareStorageImplTest {

	Configuration config = new Configuration();
/*
	@Before
	public void before() {
		StorageFactory.setType(StorageFactory.StorageType.LIVE);
		config.setLiveProjectName("TEST");
		config.setLiveQueueName("WEBCARE");
		config.setLiveServerHost("localhost");
		config.setLiveServerPort(5672);
	}

	@Test
	public void testStore() throws StorageException {

		// Add an indicator
		Storage s = StorageFactory.get();
		s.store(new Value("indicator1", "1", IndicatorType.COUNT));
		s.commit(config);
	}

	@Test(expected = IllegalArgumentException.class)
	public void testParam1() throws StorageException {
		Storage s = StorageFactory.get();
		s.store(new Value("indicator1", "1", IndicatorType.COUNT));
		s.commit(config);
	}

	@Test(expected = IllegalArgumentException.class)
	public void testParam4() throws StorageException {
		Storage s = StorageFactory.get();
		s.store(new Value("indicator1", "1", IndicatorType.COUNT));
		s.commit(config);
	}

	@Test(expected = IllegalArgumentException.class)
	public void testParam2() throws StorageException {
		Storage s = StorageFactory.get();
		s.store(new Value("indicator1", "1", IndicatorType.COUNT));
		s.commit(config);
	}

	@Test(expected = IllegalArgumentException.class)
	public void testParam3() throws StorageException {
		Storage s = StorageFactory.get();
		s.store(new Value("indicator1", "1", IndicatorType.COUNT));
		s.commit(config);
	}
	*/
}
